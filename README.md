# My Python Learning Journey

一个用于记录我 Python 学习历程的仓库。这里存放了我在学习过程中编写的程序，包括课堂作业、练习代码和我认为写得比较满意的项目。

本仓库将随着学习不断更新，用于展示我的学习成果与成长轨迹。

---

# 项目内容

目前仓库包含以下内容：

### 1.零食投票统计程序
利用上节课写过的程序，用于输入多种零食的票数并统计：

- 总票数  
- 最高票数  
- 最受欢迎的零食  
- 每种零食的排序与百分比  

示例代码如下：

```python
def snack_vote():
    # 统计零食和对应的票数
    snacks = {
        '爆米花': int(input("爆米花票数：")),
        '薯片': int(input("薯片票数：")),
        '巧克力': int(input("巧克力票数：")),
        '紫菜': int(input("紫菜票数："))
    }

    # 统计票数
    total = sum(snacks.values())
    max_votes = max(snacks.values())
    favorite = [s for s, v in snacks.items() if v == max_votes]

    # 输出统计结果
    print(f"\n总票数：{total}")
    print(f"最高票：{max_votes}")
    print(f"最受欢迎：{'、'.join(favorite)}")

    # 输出排名
    print("\n排名：")
    for i, (s, v) in enumerate(sorted(snacks.items(), key=lambda x: x[1], reverse=True), 1):

        percentage = (v / total) * 100
        # 输出排名、零食名、票数和百分比
        print(f"{i}. {s} {v}票 ({percentage:.1f}%)")
```

---

# 如何运行

### 方法一：直接运行 Python 文件
然后在命令行进入项目目录：

```
python 大家最爱的零食.py
```

按照提示输入票数即可。

---

# 代码说明

程序整体逻辑分成三部分：

### 1. 输入数据  
使用 `input()` 收集爆米花、薯片、巧克力、紫菜的票数。

### 2. 数据处理  
- 使用 `sum()` 计算总票数  
- 使用 `max()` 找到最高票数  
- 使用列表推导找出“最受欢迎”的零食  
- 使用 `sorted(..., reverse=True)` 实现排序

### 3. 输出结果  
包括总票数、最高票、最受欢迎零食以及详细排名与百分比。

---

# 学习心得与规划

在浏览 GitHub 上其他人的 Python 项目时，我有以下启发：
一个好的仓库不仅存代码，更展示结构化的思维。很多项目都有清晰的 README、明确的文件结构，以及对使用方法友好的说明。这让我意识到：写程序不仅是功能能跑，更要让别人一看就懂。
管理自己的代码非常重要。以前我把代码散落在电脑不同文件夹，现在用 GitHub 可以统一管理、版本控制，也避免丢失或混乱。
展示个人技能是长期投入的结果。很多作者将自己的学习路径、项目成长、遇到的困难记录在仓库里，这让我觉得：一个仓库可以成为自己的“学习档案”。

---

## 未来规划（My Future Plan）

我计划：

- **1.可以把我以后学习到的代码整理进来**  
- **2.可以建立项目的文件夹，进行分类**  
- **3.能够在每个项目以后都写上反思和心得，让自己做的更好**  
- **4.以后无论是用python还是matlab都可以用这个GitHub**

---

这个仓库将成为我 Python 学习旅程的完整记录。
